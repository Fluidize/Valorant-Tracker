import requests
import tkinter as tk
import urllib.parse as encode

global status
global errormsg
global region
global puuid

# IMPORTANT VARIABLES
a = input("a")
if a == "a":
    username = "fluidizeツ"
    tagline = "bonk"
    info = requests.get(
        "https://api.henrikdev.xyz/valorant/v1/account/{}/{}".format(
            encode.quote(username), encode.quote(tagline)))
    print(info.json())

# get basic user data


def getInfo():
    global info
